<?xml version="1.0" encoding="utf-8"?>
<!--
  Scoped as narrowly as the job allows.

  canRetrieveWindowContent is required to read the address bar of another browser, which
  is what closes the "you deleted the app but its in-app browser still opens the site"
  side door (§3.5). It is a powerful permission and the app says so in plain words on the
  screen that asks for it, rather than burying it.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagReportViewIds|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="200" />

*.iml
.gradle/
build/
local.properties
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Signing material. Losing these means no more in-place updates; leaking them means
# somebody else can sign an APK that installs over yours.
*.jks
*.keystore
keystore.properties
# The base64 form of the keystore is still the keystore.
*.base64
*.b64

# Build outputs
*.apk
*.aab
